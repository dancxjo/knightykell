#cloud-config
hostname: <hostname>
manage_etc_hosts: true
package_update: true
packages:
  - curl
runcmd:
  - bash -lc "curl -fsSL https://raw.githubusercontent.com/dancxjo/knightykell/refs/heads/main/scripts/install.sh | sudo bash -s -- -o dancxjo -r knightykell -b main"
